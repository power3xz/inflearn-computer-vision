import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'inflearn-computer-vision',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
